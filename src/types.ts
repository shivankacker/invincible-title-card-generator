export interface EditorState {
  text: string;
  showCredits: boolean;
  showWatermark: boolean;
  color: string;
  background: string;
  fontSize: number;
  outline: number;
  outlineColor: string;
  effect: string | null;
}
